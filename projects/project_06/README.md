# Python Quiz App

A simple asynchronous quiz application built with Python.  
This project uses `asyncio` and `aiohttp` to fetch quiz questions and handle asynchronous operations efficiently.

## Requirements

Install the required packages before running the app:

```bash
pip install aiohttp
```

## Execute

Execute the following command in the project directory:

```bash
python .\main.py
```
